name: isaforge
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - pydantic>=2.0
      - pydantic-settings>=2.0
      - pydantic-ai>=0.0.14
      - typer>=0.9
      - rich>=13.0
      - httpx>=0.25
      - isatools>=0.14
      - sqlalchemy>=2.0
      - aiosqlite>=0.19
      - pyyaml>=6.0
      - pypdf>=3.0
      - python-dotenv>=1.0
      - structlog>=23.0
      - aiofiles>=23.0
      # Development dependencies
      - pytest>=7.0
      - pytest-cov>=4.0
      - pytest-asyncio>=0.21
      - black>=23.0
      - ruff>=0.1
      - mypy>=1.0
      - pre-commit>=3.0
